# Frontend Dashboard

This directory contains the local React + Vite dashboard for browsing cached Marketplace listings.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run build
```

## Notes

- The frontend expects the backend API at `http://127.0.0.1:8000`.
- Keep this README short; the root `README.md` is the public project entrypoint.
